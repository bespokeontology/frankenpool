{
  "summary": "De-risk Dell<->M1 distributed inference: KV-cache layout matching, prior art, Gloo-over-Thunderbolt, TP feasibility on MPS",
  "agentCount": 5,
  "logs": [],
  "result": "# De-Risking Report: Distributed Inference Dell (RTX 4070) + M1 over Thunderbolt\n\n## 1. KV-LAYOUT CONTRACT — Feasible, with one hard gotcha per source engine\n\n**What MLX requires (target side)** [Report 1]: one `KVCache` per decoder layer; per-layer K and V are 4-D `(B, n_kv_heads, S, head_dim)` with **sequence on axis=2**; **GQA KV-head count** (Llama-3-8B = 8, not 32); dtype = model compute dtype (**bf16**); **K stored POST-RoPE, V stored un-rotated**. Injection is a supported one-liner: `cache = [KVCache() for _ in range(n_layers)]; cache[i].state = (k_i, v_i)` — the setter auto-sets `offset = S`, so positions line up.\n\n**From PyTorch/HF — FEASIBLE, near-identity** [Report 2]: HF `DynamicCache` per-layer is `[B, n_kv_heads, S, head_dim]` — **byte-for-byte the same dimension order as MLX**. No transpose. The transform is: `to_legacy_cache()` → per-layer split K/V → cast bf16 → `mx.array` → `cache[i].state=(k,v)`. Both HF and MLX use `rotate_half`/NEOX RoPE, so it is a drop-in. The **only** residual correctness risk is a possible element-interleaving subtlety MLX's `nn.RoPE` (`traditional=False`) vs HF [Report 1 risk #1] — resolved by the numerical round-trip test (below), not by reasoning.\n\n**From tinygrad/exo — FEASIBLE but surgical, do NOT use as source if avoidable** [Report 2]: exo stores ONE tensor per layer `[2, B, S, H, D]` — seq/heads **swapped** and K/V stacked on a leading axis. Worse, exo's `convert_from_huggingface.permute()` (llama.py ~L285-311) puts cached **K in a permuted head_dim basis** (interleaved/GPT-J RoPE), so tinygrad K is **NOT element-wise compatible** with MLX. To ship tinygrad→MLX you must: (a) index `[0]`/`[1]`, (b) `transpose(1,2)` → `[B,H,S,D]` + contiguous, (c) **apply the inverse head_dim permutation to K** (V is fine, never rotated), (d) cast fp16→bf16 (exo's 4070 path forces fp16 via the bf16 host-numpy patch). That K-un-permutation is the silent-garbage trap.\n\n**Contract verdict:** Feasible. **Use HF/CUDA as the prefill source** (identity layout, recommended by Report 2 and Report 3). tinygrad is the outlier on both axis order and RoPE basis — only the K-permute and dtype cast separate \"works\" from \"silent garbage.\" Mandatory metadata to bake into the wire blob and **fatal-assert on load**: `rope_theta`, `rope_scaling` (Llama-3 is scaled), `head_dim`, `n_kv_heads`, dtype, and absolute start position [Reports 1+2].\n\n## 2. Single biggest risk per approach\n\n**Approach A (Disaggregated, prefill-CUDA → KV stream → decode-MLX):**\nBiggest risk = **silent positional/RoPE-basis corruption across the seam** [Report 2 \"CRITICAL incompatibility\"; Report 1 risk #1]. Positions are baked into the rotated K; any mismatch in rope_theta/scaling or absolute offset corrupts all downstream attention with no error. **Surmountable: YES, cheaply.** Because HF→MLX layout is identical, the only thing to verify is RoPE, and it's verifiable with a deterministic round-trip test (prefill 10 tokens on CUDA, inject to MLX, assert top-1 logit matches a single-engine MLX run) [Reports 1+2 both recommend exactly this]. The novelty caveat [Report 3]: a general PyTorch→MLX KV bridge is **unbuilt as shipped code** — you're writing new plumbing, but the mechanism is well-understood.\n\n**Approach B (Tensor-parallel, all-reduce over Gloo):**\nBiggest risk = **Gloo cannot operate on MPS tensors at all** [Report 4; PyTorch issues #160731/#160732 — `\"No backend type associated with device type mps\"`]. Every TP layer needs ~2 all-reduces, and each forces a **Metal→CPU→TCP→CPU→Metal** round-trip on the M1 side. Combined with Gloo running collectives sequentially/CPU-staged (the same under-utilized-cable pathology the kitchen already measured) and **no `all_to_all`** support, this \"partly negates the point of pooling\" [Report 4 risks]. **Surmountable: only for correctness, NOT for performance.** It will run, but the per-layer CPU round-trip dominates latency; this is the high-risk, low-reward path.\n\n## 3. Revised build plan — Approach A (the lower-risk path)\n\nPrefill on Dell in **HF/PyTorch/CUDA** (avoids the unreliable mlx-cuda-on-4070 backend [Report 3 risk]); decode on M1 in **mlx-lm**. This is both the lower-risk build and the genuinely novel claim [Report 3].\n\n1. **Free reproduction probe first** [Report 3 rec]: install EXO 1.0 on Dell+M1, try its disaggregated path. Either it gives the win for free, or it exposes the mlx-cuda/4070 failure that justifies the custom seam. Cheap, decisive. (Note honestly: NVIDIA+Apple disagg is now **prior art** — EXO runs MLX on both sides via mlx-cuda; your novelty is keeping CUDA on PyTorch and bridging into MLX.)\n2. **Pin versions / pick one model.** Same model weights both ends. Verify `not hasattr(model, 'make_cache')` (plain `KVCache`) — **bail if the model uses RotatingKVCache / sliding-window / MoE** [Report 1 risk]. Record config: `rope_theta`, `rope_scaling`, `n_kv_heads`, `head_dim`, n_layers.\n3. **Single-engine MLX baseline.** Run mlx-lm normally on a 10-token prompt; dump `cache[i].state` for all layers. This is your golden reference.\n4. **CUDA prefill exporter.** On Dell: HF forward over the same prompt, `to_legacy_cache()`, per-layer split K/V, cast bf16, write safetensors with the config metadata. K is already POST-RoPE in HF.\n5. **MLX importer + assertions.** On M1: load blob, fatal-assert metadata vs `model.config`, build `[KVCache() for _ in n_layers]`, `cache[i].state = (mx.array(k).astype(mx.bfloat16), mx.array(v).astype(mx.bfloat16))`. Confirm dtype via `model.layers[0].self_attn.q_proj.weight.dtype`.\n6. **Round-trip correctness gate** [Reports 1+2]: decode 1 token from the injected cache; assert identical argmax/logits vs step 3's golden run. **This catches the RoPE-interleave and offset bugs immediately.** Do not proceed until green.\n7. **Wire transport.** Replace file with Thunderbolt-IP socket; stream **layer-by-layer, overlapping transfer with remaining prefill compute** (EXO's pattern). Pin the TB NIC explicitly; do not auto-select.\n8. **Honest benchmark** [Report 3 rec]: measure with **realistic long-output decode**, NOT EXO's 8k-prompt/32-token shape. Expect the win only on long-context/prefill-heavy; gains shrink decode-bound — consistent with the kitchen's prior pooling finding.\n\n## 4. Showstoppers to settle BEFORE writing code\n\n- **Model cache type** [Report 1]: if the chosen model overrides `make_cache` (Qwen3-MoE, Llama4, sliding-window), the plain-`KVCache` contract is wrong — pick a vanilla Llama/Qwen2 dense model or the whole plan breaks.\n- **RoPE scaling must match exactly** [Reports 1+2]: Llama-3 uses scaled RoPE; Qwen2 adds q/k/v bias. A mismatch is fatal and silent. Decide the model and confirm both engines apply identical scaling before exporting anything.\n- **Do NOT prefill on tinygrad/exo for the first build** [Report 2]: the K head_dim permutation + fp16 cast double-trap is avoidable by using HF/CUDA as the source. Only revisit tinygrad if HF prefill is impossible.\n- **For Approach B specifically (if ever attempted):** identical PyTorch **minor AND patch** on both hosts is a hard requirement — Gloo has no version negotiation; mismatch hangs in `init_process_group` or fails deserialization with no clear message [Report 4]. And architect every shard boundary as `.cpu()`→collective→`.to('mps')`; never pass MPS tensors to Gloo. Avoid any `all_to_all`-based scheme.\n- **Framing honesty** [Report 3]: do not claim the disaggregated NVIDIA+Apple *pattern* as novel (EXO shipped it). The defensible novel claim is a **true two-engine PyTorch/CUDA→MLX KV handoff** on commodity 4070+M1 — which Report 3 confirms does not exist as shipped code. LMCache is the production reference for the transfer *mechanism* (connector API, pinned-buffer/PD roles) but is NVIDIA-only.\n\n**Bottom line:** Approach A is materially lower-risk. The KV-layout contract is satisfiable as a near-identity reshape from HF (Report 2), the one real hazard (RoPE basis) is gated by a cheap deterministic test (Report 1), and it sidesteps both Gloo's MPS wall (Report 4) and the unreliable mlx-cuda-on-4070 path (Report 3). Approach B is correct-but-slow at best and should be deprioritized.",
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Research"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Synthesize"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "research:mlx-kv",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "a9113d4c5885e99f1",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1782771460152,
      "queuedAt": 1782771460124,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "How Apple MLX (mlx-lm) represents and consumes the KV cache…",
      "promptPreview": "Research how Apple MLX (mlx-lm) represents and consumes the KV cache during autoregressive decode for a Llama/Qwen-family model. Goal: hand a KV cache computed by ANOTHER engine into mlx-lm's decode loop. Find the EXACT per-layer KV tensor shapes and dtype, the KVCache class in mlx-lm, whether RoPE is applied before or after caching, and the precise point decode reads the cache. Use WebSearch and …",
      "lastProgressAt": 1782771547545,
      "tokens": 19228,
      "toolCalls": 6,
      "durationMs": 87393,
      "resultPreview": "{\"topic\":\"How Apple MLX (mlx-lm) represents and consumes the KV cache during autoregressive decode for Llama/Qwen-family models, for handing in an externally-computed KV cache\",\"keyFindings\":[\"KV cache class is `KVCache` in `mlx_lm/models/cache.py` (subclass of `_BaseCache`). Per layer there is ONE KVCache instance; `make_prompt_cache(model)` returns `[KVCache() for _ in range(num_layers)]` (one p…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "research:torch-kv",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "ac2aef234494b9832",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1782771460154,
      "queuedAt": 1782771460124,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "KV-cache representation in HF transformers (PyTorch) vs tin…",
      "promptPreview": "Research how PyTorch (HuggingFace transformers) and tinygrad represent the KV cache during LLM prefill for a Llama/Qwen model. Goal: serialize the post-prefill KV cache and ship it to another engine (MLX). Find HF's DynamicCache / past_key_values exact format (shapes, dtype, per-layer tuple of (K,V)), how RoPE interacts with what's cached, and tinygrad's exo KV layout. Use WebSearch + read source.…",
      "lastProgressAt": 1782771590436,
      "tokens": 35105,
      "toolCalls": 13,
      "durationMs": 130282,
      "resultPreview": "{\"topic\":\"KV-cache representation in HF transformers (PyTorch) vs tinygrad/exo vs MLX, and serializing a post-prefill cache for cross-engine transfer to MLX (Llama/Qwen)\",\"keyFindings\":[\"HF DynamicCache stores, per layer, a separate key tensor and value tensor each shaped [batch, num_key_value_heads, seq_len, head_dim]. update() grows them by torch.cat on dim=-2 (the seq dim). Legacy past_key_valu…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "research:prior-art",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "af55233ffde310b62",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1782771460155,
      "queuedAt": 1782771460124,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Prior art for disaggregated/cross-engine heterogeneous (NVI…",
      "promptPreview": "Search for PRIOR ART, skeptically. (1) Disaggregated LLM inference (prefill/decode split) across HETEROGENEOUS hardware. (2) Cross-engine KV-cache handoff (PyTorch->MLX or any framework pair). (3) NVIDIA GPU + Apple Silicon running ONE model distributed. (4) exo-explore's disaggregated/KV-streaming mode. For each: does a REAL implementation exist (not just a blog), who, how, what's the KV-transfer…",
      "lastProgressAt": 1782771607516,
      "tokens": 29304,
      "toolCalls": 15,
      "durationMs": 147361,
      "resultPreview": "{\"topic\":\"Prior art for disaggregated/cross-engine heterogeneous (NVIDIA + Apple Silicon) LLM inference and KV-cache handoff\",\"keyFindings\":[\"EXO 1.0 (exo-explore) is the closest real prior art to the kitchen's exact scope: it ran prefill on an NVIDIA DGX Spark and decode on an Apple M3 Ultra Mac Studio, streaming the KV cache layer-by-layer over a 10GbE link, claiming a 2.8x end-to-end speedup on…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "research:gloo-tb",
      "phaseIndex": 1,
      "phaseTitle": "Research",
      "agentId": "a49e192cc5ca2d6e8",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1782771460155,
      "queuedAt": 1782771460124,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "PyTorch torch.distributed Gloo backend over Thunderbolt-IP …",
      "promptPreview": "Research PyTorch torch.distributed with the GLOO backend connecting a Linux/x86+CUDA host and a macOS/Apple-Silicon host over an IP link (Thunderbolt-IP). Find: cross-OS/cross-arch Gloo known issues; whether peers need matching PyTorch versions (can 2.8.0 talk to a different minor?); init setup (MASTER_ADDR/PORT, GLOO_SOCKET_IFNAME to pin the thunderbolt0 interface); whether tensors must be CPU fo…",
      "lastProgressAt": 1782771607687,
      "tokens": 28418,
      "toolCalls": 16,
      "durationMs": 147532,
      "resultPreview": "{\"topic\":\"PyTorch torch.distributed Gloo backend over Thunderbolt-IP between a Linux/x86+CUDA host and a macOS/Apple-Silicon host, plus MPS op-coverage gaps for tensor-parallel transformer sharding\",\"keyFindings\":[\"Gloo is the ONLY viable cross-OS/cross-arch CPU backend. It is built into the official wheels on both Linux (since 0.2) and macOS (since 1.3). NCCL is Linux+CUDA-only, MPI requires cust…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "synthesize",
      "phaseIndex": 2,
      "phaseTitle": "Synthesize",
      "agentId": "aa18b10ababdacdc5",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1782771607688,
      "queuedAt": 1782771607688,
      "attempt": 1,
      "promptPreview": "Synthesize research to de-risk a build: distributed LLM inference across a Dell (RTX 4070, CUDA, Linux) and an M1 MacBook (Apple Silicon, macOS) over a Thunderbolt cable. Two approaches: (A) DISAGGREGATED - prefill on Dell, stream KV cache one-way to M1, decode on M1; (B) TENSOR-PARALLEL - split every layer, all-reduce over Gloo.\n\nResearch reports (JSON):\n[\n  {\n    \"topic\": \"How Apple MLX (mlx-lm)…",
      "lastProgressAt": 1782771679967,
      "tokens": 27561,
      "toolCalls": 0,
      "durationMs": 72278,
      "resultPreview": "# De-Risking Report: Distributed Inference Dell (RTX 4070) + M1 over Thunderbolt\n\n## 1. KV-LAYOUT CONTRACT — Feasible, with one hard gotcha per source engine\n\n**What MLX requires (target side)** [Report 1]: one `KVCache` per decoder layer; per-layer K and V are 4-D `(B, n_kv_heads, S, head_dim)` with **sequence on axis=2**; **GQA KV-head count** (Llama-3-8B = 8, not 32); dtype = model compute dtyp…"
    }
  ],
  "totalTokens": 139616,
  "totalToolCalls": 50
}